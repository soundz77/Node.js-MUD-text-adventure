const sessionMessages = Object.freeze({
  errors: {
    required: "Required parameters not provided for session config",
  },
  success: {
    connect: "Session connected successfully",
    disconnect: "Session disconnected successfully",
  },
});

export default sessionMessages;
